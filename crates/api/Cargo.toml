[package]
name = "keel-api"
default-run = "keel-api"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Keel demo HTTP process for local sidecar management"
publish = false

[[bin]]
name = "keel-api"
path = "src/main.rs"

[dependencies]
axum = { workspace = true }
keel = { workspace = true, features = ["http"] }
keel-gate = { workspace = true }
tokio = { workspace = true }
