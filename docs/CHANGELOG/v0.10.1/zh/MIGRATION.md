# 迁移到 v0.10.1

对调用方没有任何要求。公开表面未变，estate format 仍是十一，没有库需要重建。

跑在 Postgres 上就升级。在本次发布之前，可空文本列会被第一行留空的写入钉死成
`int8`，之后每一行为它提供文本的写入都会以 `insufficient data left in message` 失败。
这是拒绝而不是损坏——那次写入不会落库——但它可能在 schema 建立很久之后才出现：
就在第一行去填前面各行都留空的那个列的时候。

只跑 Sqlite 的话，本次发布不改变任何你观察得到的东西。
